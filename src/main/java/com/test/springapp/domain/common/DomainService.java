package com.test.springapp.domain.common;

/**
 * Marker interface for domain services.
 * Domain services contain domain logic that doesn't naturally fit within entities or value objects.
 * They are stateless and encapsulate domain operations.
 */
public interface DomainService {
}
